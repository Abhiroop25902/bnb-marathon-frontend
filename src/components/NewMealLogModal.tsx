import {useTheme} from "@mui/material";


export default function NewMealLogModal() {
    const theme = useTheme();
    return (
        <div style={{backgroundColor: theme.palette.background.default}}>
            NewMealLogModal
        </div>
    )
}